---
{"publish":true,"created":"2025-09-23T11:01:31.356+07:00","modified":"2025-09-23T13:03:08.667+07:00","published":"2025-09-23T13:03:08.667+07:00","cssclasses":"","creation-time":"2025-09-23","parent":["[[daily notes]]"]}
---


- Nama: Faris Ahmad Zubair Bauw
- NPM: 2396217960

## Nomor 1

### 1.1

> [!note] Soal
> Estimasi model [[3 Reference/estimating-the-model_202509091644\|OLS]] ($Y$ pada $X_{1}$, $X_{2}$). Tentukan koefisien regresi, $SE$, $R^2$.

Secara umum, [[3 Reference/estimating-the-model_202509091644\|ordinary least square]] dari suatu model dapat ditentukan oleh $\hat{\boldsymbol{\beta}}=(\mathbf{x}'\mathbf{x})^{-1}\mathbf{x}'\mathbf{y}$. Menggunakan rumus tersebut, diperoleh $\hat{\boldsymbol{\beta}}$ sebagai berikut:
$$
\begin{align}
\hat{\boldsymbol{\beta}}  & = \begin{bmatrix} 2.2683446 & 0.2625189 & 0.4693932 \end{bmatrix}' \\
\end{align}.
$$

sehingga diperoleh $\beta_{0}=2.2683446$.

[[3 Reference/standard-error_202509231153\|Standard error]] dapat dicari dengan rumus berikut:
$$SE(\hat{\boldsymbol{\beta}}) = \sqrt{\text{diag}(\hat{\sigma}^2 (\mathbf{x}'\mathbf{x})^{-1})}$$

dimana:
$$
\begin{align}
\hat{\sigma}^2 & = \frac{SSE}{n-k} \\
SSE & = \sum_{i=1}^{n} (y_i - \hat{y}_i)^2
\end{align}
$$

Dari rumus tersebut, diperoleh $SE(\hat{\boldsymbol{\beta}})$ sebagai berikut:
$$
\begin{align}
SE(\beta_{0}) &= 0.13911 \\
SE(\beta_{1}) &= 0.03320\\
SE(\beta_{2}) &= 0.32665
\end{align}
$$



[[3 Reference/coefficient-of-determination_202509231125\|Coefficient of determination]] ($R^2$) dapat dicari dengan rumus berikut:
$$
\begin{align}
R^2 & = 1 - \frac{SSE}{SST} \\
 & = 1 - \frac{\sum_{i=1}^{n}(y_i - \hat{y}_i)^2}{\sum_{i=1}^{n}(y_i - \bar{y})^2}
\end{align}
$$

dimana:
$$
\begin{align}
SSE &= \sum_{i=1}^{n}(y_i - \hat{y}_i)^2 \\
SST &= \sum_{i=1}^{n}(y_i - \bar{y})^2 \\
\hat{y}_i &= X_i\hat{\beta}
\end{align}
$$

Dari rumus tersebut, diperoleh:
$$
R^2=0.99772
$$

### 1.2

> [!note] Soal
> Lakukan uji heteroskedastisitas: Breusch-Pagan dan/atau White test. Jelaskan hasil (statistik & p-value).

Dari uji Breusch-Pagan diperoleh p-value sebagai berikut:
- Lagrange Multiplier p-value: $0.014935$
- F p-value: $0.009695$

Dari uji White diperoleh p-value sebagai berikut:
- Lagrange Multiplier p-value: $0.045105$
- F p-value: $0.024992$

Dari semua hasil tersebut, dapat disimpulkan bahwa terdapat bukti yang cukup untuk mengatakan bahwa error bersifat heteroskedastisitas, sehingga melanggar [[3 Reference/assumptions-for-the-error-component_202509091314#3. Constant variance assumption (homoscedasticity)\|asumsi homoskedastisitas]].

### 1.3
> [!note] Soal 
> Bila heteroskedastisitas ditemukan, berikan dua solusi: 
> 1. SE heteroskedastik-konsisten (robust SE / White), 
> 2. Estimasi GLS. 

Diperoleh hasil sebagai berikut:

| Variable  | OLS Coef | OLS SE | Robust SE | GLS Coef | GLS SE |
| --------- | -------- | ------ | --------- | -------- | ------ |
| Intercept | 2.268    | 0.139  | 0.255     | 2.180    | 0.082  |
| $X_{1}$   | 0.263    | 0.033  | 0.065     | 0.286    | 0.026  |
| $X_2$     | 0.469    | 0.327  | 0.649     | 0.285    | 0.248  |
| $R^2$     | 0.9977   |        |           | 0.9985   |        |

- **Robust SE**: Koefisien tidak berubah, tetapi kesalahan standar meningkat signifikan, membuat $X_2$ tidak signifikan secara statistik (nilai-p meningkat).
- **GLS**: Koefisien dan kesalahan standar berubah. Koefisien $X_2$ turun dari 0.469 ke 0.285, dan GLS memberikan kesalahan standar lebih kecil daripada robust SE.

GLS menghasilkan $R^2$ lebih tinggi (0.9985 vs 0.9977), menunjukkan kecocokan lebih baik.

Gunakan robust standard errors untuk inferensi konservatif tanpa asumsi kuat tentang struktur varians, atau GLS jika efisiensi penting dan model varians spesifik.

### 1.4
> [!note] Soal
> Plot residual squared vs $X_{1}$ dan interpretasikan hasilnya.

![[assets/image-23.png]]

Plot menunjukkan hubungan positif antara pendapatan ($X_{1}$) dan squared residuals. Garis tren naik mengindikasikan bahwa tingkat pendapatan yang lebih tinggi berhubungan dengan penyimpangan lebih besar dari prediksi model, yang mungkin menunjukkan heteroskedastisitas.

## Nomor 2.

## Nomor 1
### 1.1
> [!note] Soal
> Estimasi model OLS (Y pada $X_1$, $X_2$). Tentukan koefisien regresi, SE, $R^2$.
Estimasi koefisien regresi OLS adalah:
- $\hat{\beta}_0 = 2.268$
- $\hat{\beta}_1 = 0.263$
- $\hat{\beta}_2 = 0.469$
Kesalahan standar (SE) masing-masing koefisien adalah:
- $SE(\hat{\beta}_0) = 0.139$
- $SE(\hat{\beta}_1) = 0.033$
- $SE(\hat{\beta}_2) = 0.327$
Koefisien determinasi ($R^2$) adalah 0.998.

### 1.2
> [!note] Soal
> Lakukan uji heteroskedastisitas: Breusch-Pagan dan/atau White test. Jelaskan hasil (statistik & p-value).
Hasil uji Breusch-Pagan menunjukkan:
- Lagrange Multiplier p-value: 0.015
- F p-value: 0.010
Hasil uji White menunjukkan:
- Lagrange Multiplier p-value: 0.045
- F p-value: 0.025
Semua p-value di bawah 0.05, sehingga terdapat bukti yang cukup untuk menolak hipotesis nol, menunjukkan adanya heteroskedastisitas.

### 1.3
> [!note] Soal
> Bila heteroskedastisitas ditemukan, berikan dua solusi: (i) SE heteroskedastik-konsisten (robust SE / White), (ii) estimasi GLS.
Jika heteroskedastisitas ditemukan, solusi yang dapat digunakan adalah:
- **SE Robust (White)**: Menggunakan kesalahan standar yang disesuaikan untuk heteroskedastisitas, meningkatkan keandalan inferensi.
- **Estimasi GLS**: Menyesuaikan model dengan struktur varians yang diperkirakan, meningkatkan efisiensi estimasi.

### 1.4
> [!note] Soal
> Plot residual squared vs $X_1$ dan interpretasikan hasilnya.
Plot menunjukkan hubungan positif antara $X_1$ dan residual squared. Garis tren yang naik mengindikasikan bahwa variansi residual meningkat seiring dengan kenaikan $X_1$, yang mendukung adanya heteroskedastisitas.

## Nomor 2
### 2.1
> [!note] Soal
> Estimasi OLS $Y_t = \alpha + \beta X_t + u_t$. Tentukan koefisien regresi dan SE.
Estimasi koefisien regresi OLS adalah:

- $\hat{\alpha} = -1.367$
- $\hat{\beta} = 0.040$
Kesalahan standar (SE) masing-masing koefisien adalah:
- $SE(\hat{\alpha}) = 0.402$
- $SE(\hat{\beta}) = 0.003$

### 2.2
> [!note] Soal
> Uji autokorelasi: Durbin-Watson dan/atau Breusch-Godfrey (lag 1 atau lag 2). Jelaskan hasil. (Gunakan aturan interpretasi DW).

- Statistik Durbin-Watson: 1.00
- p-value Breusch-Godfrey (lag 1): 0.017
- p-value Breusch-Godfrey (lag 2): 0.042
Statistik DW sebesar 1.00 di bawah nilai kritis bawah (sekitar 1.5 untuk n=30, k=1), menunjukkan adanya autokorelasi positif. p-value dari uji Breusch-Godfrey yang kurang dari 0.05 juga mengkonfirmasi adanya autokorelasi.

### 2.3
> [!note] Soal
> Jika autokorelasi terdeteksi, terapkan metode GLS untuk model dengan error AR(1) (mis. estimasi $\rho$ dari residual, lalu pakai Cochrane-Orcutt atau Prais-Winsten). Tunjukkan $\hat{\rho}$, koefisien GLS dan bandingkan dengan OLS.

- Estimasi $\hat{\rho} = 0.47$
- Koefisien GLS (Cochrane-Orcutt):
  - $\hat{\alpha} = 0.562$
  - $\hat{\beta} = -0.004$
- Perbandingan dengan OLS:
  - OLS: $\hat{\alpha} = -1.367$, $\hat{\beta} = 0.040$
  - GLS: $\hat{\alpha} = 0.562$, $\hat{\beta} = -0.004$
Koefisien GLS berbeda signifikan dari OLS, menunjukkan penyesuaian untuk autokorelasi AR(1).

### 2.4
> [!note] Soal
> Interpretasikan mengapa autokorelasi mempengaruhi inferensi OLS (efisiensi & SE).

Autokorelasi melanggar asumsi kesalahan independen dalam OLS, menyebabkan estimasi yang tidak efisien dan kesalahan standar yang diremehkan. Hal ini meningkatkan risiko kesalahan Tipe I dalam pengujian hipotesis, sehingga inferensi OLS menjadi tidak dapat diandalkan.

Source code:
```python
from statsmodels.stats.diagnostic import acorr_breusch_godfrey
from statsmodels.stats.diagnostic import het_breuschpagan, het_white
from statsmodels.stats.stattools import durbin_watson
import matplotlib.pyplot as plt
import numpy as np
import pandas as pd
import statsmodels.api as sm

#|%%--%%| <lLbIHN0CgD|Gy0SzloQlg>
r"""°°°
# 1
°°°"""
#|%%--%%| <Gy0SzloQlg|Yq7ywIVKZq>
data1 = pd.DataFrame({
    'id': range(1, 21),
    'Y': [5.2, 6.1, 6.9, 7.0, 7.8, 8.5, 9.0, 10.2, 10.6, 11.0, 11.7, 12.5, 13.3, 14.0, 15.5, 16.8, 17.4, 18.2, 19.0, 20.5],
    'X1': [10, 12, 14, 15, 18, 20, 22, 25, 27, 28, 30, 32, 35, 38, 42, 45, 48, 50, 55, 60],
    'X2': [1.0, 1.5, 1.2, 1.8, 2.0, 2.1, 2.4, 2.7, 3.0, 3.2, 3.4, 3.6, 4.0, 4.1, 4.4, 4.8, 5.0, 5.3, 5.6, 6.0]
})

#|%%--%%| <Yq7ywIVKZq|OOmmCIt03b>
r"""°°°
## 1.1

Estimasi model OLS (Y pada X1, X2). Tentukan koefisien regresi, SE, R2. 
°°°"""
#|%%--%%| <OOmmCIt03b|SswMc9GgKV>

y = data1['Y'].values.reshape(-1, 1)
x = np.column_stack([np.ones(len(data1)), data1['X1'], data1['X2']])
xt = x.T

# b = (x'x)^-1 x' y
beta_hat = np.linalg.inv(xt @ x) @ xt @ y

# residuals
y_hat = x @ beta_hat
residuals = y - y_hat

# SSE (sum of squared errors)
SSE = (residuals.T @ residuals)[0,0]  # to scalar

n = x.shape[0]  # n observations
k = x.shape[1]  # n params

# residual var
sigma_squared = SSE / (n - k)

# standard error
variance_covariance = sigma_squared * np.linalg.inv(x.T @ x)
standard_errors = np.sqrt(np.diag(variance_covariance))

# R-squared
y_mean = np.mean(y)
SST = ((y - y_mean).T @ (y - y_mean))[0][0]
R_squared = 1 - SSE/SST

print("Regression coefficients:")
print(f"Intercept (beta0): {beta_hat[0,0]:.5f}")
print(f"beta1: {beta_hat[1,0]:.5f}")
print(f"beta2: {beta_hat[2,0]:.5f}")

print("Standard errors:")
print(f"    SE(beta0): {standard_errors[0]:.5f}")
print(f"    SE(beta1): {standard_errors[1]:.5f}")
print(f"    SE(beta2): {standard_errors[2]:.5f}")

print(f"R-squared: {R_squared:.5f}")
#|%%--%%| <SswMc9GgKV|1PRz6z9O4h>
r"""°°°
## 1.2

Lakukan uji heteroskedastisitas: Breusch-Pagan dan/atau White test. Jelaskan hasil 
°°°"""
#|%%--%%| <1PRz6z9O4h|qAQmKBzmM5>

# Convert to proper format for statsmodels
X_sm = x[:, 1:]  # Remove intercept column (statsmodels adds it automatically)
y_sm = y.flatten()  # Convert to 1D array

# Fit OLS model using statsmodels
model = sm.OLS(y_sm, sm.add_constant(X_sm)).fit()


# Breusch-Pagan Test
print("\n1. BREUSCH-PAGAN TEST")
print("-" * 30)
_, bp_lm_pvalue, _, bp_f_pvalue = het_breuschpagan(model.resid, model.model.exog)

print(f"LM p-value: {bp_lm_pvalue:.6f}")
print(f"F p-value: {bp_f_pvalue:.6f}")

print("\nInterpretation:")
print("H0: Homoskedasticity (constant error variance)")
print("H1: Heteroskedasticity")
if bp_lm_pvalue < 0.05:
    print(f"Decision: REJECT H0 (p-value = {bp_lm_pvalue:.6f} < 0.05)")
    print("Conclusion: Evidence of heteroskedasticity")
else:
    print(f"Decision: FAIL TO REJECT H0 (p-value = {bp_lm_pvalue:.6f} ≥ 0.05)")
    print("Conclusion: No evidence of heteroskedasticity")

# White Test
print("\n\n2. WHITE TEST")
print("-" * 20)
_, white_lm_pvalue, _, white_f_pvalue = het_white(model.resid, model.model.exog)

print(f"LM p-value: {white_lm_pvalue:.6f}")
print(f"F p-value: {white_f_pvalue:.6f}")

print("\nInterpretation:")
print("H0: Homoskedasticity (constant error variance)")
print("H1: Heteroskedasticity (more general form)")
if white_lm_pvalue < 0.05:
    print(f"Decision: REJECT H0 (p-value = {white_lm_pvalue:.6f} < 0.05)")
    print("Conclusion: Evidence of heteroskedasticity")
else:
    print(f"Decision: FAIL TO REJECT H0 (p-value = {white_lm_pvalue:.6f} ≥ 0.05)")
    print("Conclusion: No evidence of heteroskedasticity")

print("\n" + "="*60)
print("SUMMARY")
print("="*60)
print("Breusch-Pagan: Tests if variance is linear function of X variables")
print("White Test: More general test including squares and cross-products")
print("Both tests use LM statistic ~ χ² distribution")
print("Reject H0 if p-value < 0.05 (evidence of heteroskedasticity)")
#|%%--%%| <qAQmKBzmM5|p8EtqgfNwu>
r"""°°°
## 1.3

Bila heteroskedastisitas ditemukan, berikan dua solusi: 

1. SE heteroskedastik-konsisten (robust SE / White), 
2. Estimasi GLS. 
°°°"""
#|%%--%%| <p8EtqgfNwu|wyoJ83kWW9>

# SOLUTION 1: Heteroskedasticity-Consistent Standard Errors (White/Robust SE)
print("\n1. ROBUST STANDARD ERRORS (White/HC)")
print("-" * 40)

# Fit model with robust standard errors
model_robust = sm.OLS(y_sm, sm.add_constant(X_sm)).fit(cov_type='HC0')

print("Comparison of Standard Errors:")
print(f"{'Variable':<12} {'OLS SE':<10} {'HC0 SE':<10}")
print("-" * 52)
variables = ['Intercept', 'X1', 'X2']
for i, var in enumerate(variables):
    ols_se = model.bse[i]
    hc0_se = model_robust.bse[i]
    print(f"{var:<12} {ols_se:<10.6f} {hc0_se:<10.6f}")

#|%%--%%| <wyoJ83kWW9|UnjnEzhNCm>

print("\n\n2. GENERALIZED LEAST SQUARES (GLS)")
print("-" * 40)

# Get absolute residuals to avoid log(negative)
abs_residuals = np.abs(model.resid)
log_resid_sq = np.log(abs_residuals**2)

# Regress log(residuals²) on X variables
variance_model = sm.OLS(log_resid_sq, sm.add_constant(X_sm)).fit()

log_sigma_sq_hat = variance_model.predict()
sigma_sq_hat = np.exp(log_sigma_sq_hat)

sigma_matrix = np.diag(sigma_sq_hat)  # Diagonal covariance matrix
model_gls = sm.GLS(y_sm, sm.add_constant(X_sm), sigma=sigma_matrix).fit()

print("GLS Results:")
print(model_gls.summary().tables[1])

# Comparison table
print("\n" + "="*60)
print("COMPARISON: OLS vs ROBUST SE vs GLS")
print("="*60)

print(f"{'Variable':<12} {'OLS Coef':<12} {'OLS SE':<10} {'GLS Coef':<12} {'GLS SE':<10}")
print("-" * 70)

variables = ['Intercept', 'X1', 'X2']
for i, var in enumerate(variables):
    ols_coef = model.params[i]
    ols_se = model.bse[i]
    gls_coef = model_gls.params[i]
    gls_se = model_gls.bse[i]
    print(f"{var:<12} {ols_coef:<12.6f} {ols_se:<10.6f} {gls_coef:<12.6f} {gls_se:<10.6f}")

print(f"\nModel Fit Comparison:")
print(f"OLS R²: {model.rsquared:.6f}")
print(f"GLS R²: {model_gls.rsquared:.6f}")
#|%%--%%| <UnjnEzhNCm|x159zVQaNZ>
r"""°°°
## 1.4
°°°"""
#|%%--%%| <x159zVQaNZ|FPGAeX1F1c>

residuals_squared = model.resid**2

plt.figure(figsize=(10, 6))
plt.scatter(data1['X1'], residuals_squared)

z = np.polyfit(data1['X1'], residuals_squared, 1)
p = np.poly1d(z)
plt.plot(data1['X1'], p(data1['X1']), "r--", label='Trend line')
plt.legend()

plt.xlabel('$X_1$')
plt.ylabel('Squared Residuals ($e^2$)')
plt.title('Squared Residuals vs $X_1$')
plt.grid(True)

plt.savefig("1_4.png")

# Print numerical analysis
print("="*60)
print("RESIDUAL ANALYSIS")
print("="*60)
print(f"Correlation between X1 and e²: {correlation:.6f}")
print(f"Range of squared residuals: {residuals_squared.min():.6f} to {residuals_squared.max():.6f}")
print(f"Mean squared residual: {residuals_squared.mean():.6f}")
print(f"Std dev of squared residuals: {residuals_squared.std():.6f}")

#|%%--%%| <FPGAeX1F1c|9Tl3QRNWLT>
r"""°°°
# 2
°°°"""
#|%%--%%| <9Tl3QRNWLT|KEPbTZDNiK>
data2 = pd.DataFrame({
    't': range(1, 31),
    'Year': range(1991, 2021),
    'X': [100, 102, 105, 107, 110, 112, 115, 118, 120, 122, 125, 127, 130, 132, 134, 137, 139, 140, 142, 144, 146, 148, 150, 152, 154, 156, 158, 160, 162, 164],
    'Y': [2.1, 2.5, 2.8, 3.0, 3.4, 3.6, 3.9, 2.8, 3.2, 3.5, 3.7, 3.9, 4.1, 4.3, 4.5, 4.6, 4.7, 3.9, 4.0, 4.2, 4.4, 4.5, 4.6, 4.7, 4.8, 4.9, 5.0, 5.1, 5.2, 5.3]
})

#|%%--%%| <KEPbTZDNiK|r318iZye5V>
r"""°°°
## 2.1

Estimasi OLS $Y_t = \alpha + \beta X_t + u_t$. Tentukan koefisien regresi dan SE.
°°°"""
#|%%--%%| <r318iZye5V|B8HYzOEDWR>

y = data2['Y'].values
X = sm.add_constant(data2['X'].values)  # Add intercept
model_ols = sm.OLS(y, X).fit()

print("Regression coefficients:")
print(f"Intercept (alpha): {model_ols.params[0]:.3f}")
print(f"beta: {model_ols.params[1]:.3f}")

print("Standard errors:")
print(f"    SE(alpha): {model_ols.bse[0]:.3f}")
print(f"    SE(beta): {model_ols.bse[1]:.3f}")

#|%%--%%| <B8HYzOEDWR|02FfjdutAu>
r"""°°°
## 2.2

Uji autokorelasi: Durbin-Watson dan/atau Breusch-Godfrey (lag 1 atau lag 2). Jelaskan hasil.
°°°"""
#|%%--%%| <02FfjdutAu|cXPs7A4cqS>

# Durbin-Watson Test
dw_stat = durbin_watson(model_ols.resid)
print("\nDurbin-Watson Test:")
print(f"DW Statistic: {dw_stat:.2f}")
print("Interpretation: DW < 1.5 suggests positive autocorrelation (n=30, k=1)")

# Breusch-Godfrey Test (lag 1 and lag 2)
bg_test_1 = acorr_breusch_godfrey(model_ols, nlags=1)
bg_test_2 = acorr_breusch_godfrey(model_ols, nlags=2)
print("\nBreusch-Godfrey Test:")
print(f"Lag 1 p-value: {bg_test_1[1]:.3f}")
print(f"Lag 2 p-value: {bg_test_2[1]:.3f}")
print("Interpretation: p-value < 0.05 indicates autocorrelation")

#|%%--%%| <cXPs7A4cqS|fSSwZjE3aU>
r"""°°°
## 2.3

Jika autokorelasi terdeteksi, terapkan metode GLS untuk model dengan error AR(1) (mis. estimasi ρ dari residual, lalu pakai Cochrane-Orcutt atau Prais-Winsten). Tunjukkan ^ρ, koefisien GLS dan bandingkan dengan OLS.
°°°"""
#|%%--%%| <fSSwZjE3aU|8feb1poL7l>

# Estimate rho from residuals
residuals = model_ols.resid
rho = np.corrcoef(residuals[:-1], residuals[1:])[0, 1]
print(f"\nEstimated rho (^ρ): {rho:.2f}")

# Cochrane-Orcutt transformation
y_trans = residuals[1:] - rho * residuals[:-1]
X_trans = X[1:] - rho * X[:-1]
model_gls = sm.OLS(y_trans, X_trans).fit()

print("\nGLS Coefficients (Cochrane-Orcutt):")
print(f"Intercept (alpha): {model_gls.params[0]:.3f}")
print(f"beta: {model_gls.params[1]:.3f}")

print("\nComparison with OLS:")
print(f"OLS: alpha = {model_ols.params[0]:.3f}, beta = {model_ols.params[1]:.3f}")
print(f"GLS: alpha = {model_gls.params[0]:.3f}, beta = {model_gls.params[1]:.3f}")

#|%%--%%| <8feb1poL7l|Yq5rNi9r8m>
r"""°°°
## 2.4

Interpretasikan mengapa autokorelasi mempengaruhi inferensi OLS (efisiensi & SE).
°°°"""
#|%%--%%| <Yq5rNi9r8m|7vb5d7naMD>

print("\nAutocorrelation violates the OLS assumption of independent errors, leading to inefficient estimates and underestimated standard errors. This increases the risk of Type I errors in hypothesis testing, making OLS inferences unreliable.")
```