---
{"publish":true,"cssclasses":""}
---


For brevity, we implicitly let for all sections in this page
- $x$ : Element
- $A,B,C$ : Set

## Basic set notations
| Notation                          | Meaning                                                                                    |
| --------------------------------- | ------------------------------------------------------------------------------------------ |
| $x\in A$                          | $x$ is **in** $A$;<br>$x$ is a **member** of $A$;<br>$x$ **belongs** to $A$                |
| $x\not\in A$                      | $x$ **not in** $A$;                                                                        |
| $A\subseteq B$;<br>$B\supseteq A$ | Every element of $A$ also belongs to $B$;<br>$A$ is a **subset** of $B$                    |
| $A\subset B$;<br>$B\supset A$     | $A$ contains some element of $B$, but not all of $B$;<br>$A$ is a **proper subset** of $B$ |

## 1.1.1 Definition: Set equality


If $A$ and $B$ contains the same elements

Then
- We say $A$ and $B$ are **equal**
- We write $A=B$


Thus, to prove that the sets $A$ and $B$ are equal, we must show that $A\subseteq B$ and $B\subseteq A$.

## 1.1.3 Definition: Set union, intersection, complement

If  $ A\cup B := \{ x: x \in A \text{ or } x\in B \} $$
 
Then we say set $A\cup B$ is the **union** of $A$ and $B$


If $ A\cap B:=\{ x:x\in A \text{ and } x\in B\} $$

Then we say set $A\cap B$ is the **intersection** of $A$ and $B$



If $ A\setminus B:=\{ x:x \in A\text{ and } x \not \in B \} $$

Then we say set $A\setminus B$ is the **complement of $B$ relative to $A$**

Or simply:
- Union: All elements existing in $A$, but not existing in $B$
- Intersection: All elements existing in either $A$ or $B$
- Complement: All elements existing in both $A$ and $B$ at the same time

![[Assets/Pasted image 20250627022058.png]]

For unions and intersections for more than 2 sets:

#TODO
Let 
- $n\in \mathbb{N}$
- $\{ A_{1},A_{2},\dots,A_{n},\dots \}$ : Infinite collection of sets

Their **union** is the set of elements that belong to *at least one* of the sets $A_{n}$
$$
\bigcup_{n=1}^\infty A_{n} := \{ x:x \subset A_{n} \}
$$

Their **intersection** is the set of elements that belong to *all* of these sets $A_{n}$
$$
\bigcap_{n=1}^\infty A_{n} := \{ x:x \subset A_{n} \}
$$


## 1.1.4 Theorem
$$
\begin{align}
A\setminus(B\cup C) & = (A\setminus B)\cap(A\setminus C) \\
A\setminus(B\cap C) & = (A\setminus B)\cup(A\setminus C)
\end{align}
$$